# 🧭 Movie Browser (Навигация в SwiftUI)

Домашнее задание к лекции 6.  
Создайте приложение с навигацией на основе `NavigationStack`, с передачей данных между экранами и редактированием через `@Binding`.

---

## 🎯 Цели
- Освоить навигацию в SwiftUI (`NavigationStack`, `NavigationLink`).
- Научиться передавать данные в детальный экран.
- Научиться редактировать данные через `@Binding` и обновлять список.
- Понять разницу между навигацией:
  - `NavigationLink(destination:)`
  - `NavigationLink(value:)`
  - `navigationDestination(for:)`
  - `NavigationPath`
- Закрепить MVVM + Observation (`@Observable`, `@Bindable`).

---

## 📚 Техническое задание

1. Создать модель `Movie` (id, title, genre, description, releaseYear).  
2. Отобразить список фильмов на главном экране.  
3. При нажатии — переход на детальный экран.  
4. На детальном экране можно редактировать:
   - название  
   - жанр  
   - описание  
5. Изменения должны **мгновенно** отражаться в списке.  
6. Добавить возможность создавать новые фильмы (через sheet).  
7. Разделить приложение минимум на 3 вьюхи:
   - список (`MoviesListView`)
   - строка фильма (`MovieRowView`)
   - детальный экран (`MovieDetailView`)

---

## 💻 Уровни выполнения

### Base
- Навигация через `NavigationLink { … }`.
- Детальный экран принимает `@Binding`.
- Редактирование обновляет данные на главном экране.
- Добавление и удаление фильмов.

### Plus
- Использовать `NavigationPath` + `navigationDestination`.
- Поиск по названию.
- Сортировка фильмов (по году или алфавиту).

### Pro
- Сохранение фильмов в `UserDefaults`.
- Добавить постер (SF Symbols или Assets).
- Анимации появления строк.

---

## ✅ Критерии приёмки
- Фильмы можно добавлять, редактировать и удалять.
- Изменения на детальном экране отображаются в списке.
- Реализована навигация через `NavigationStack`.
- Минимум три отдельные вьюхи.
- Используется Observation (`@Observable`, `@Bindable`), **не** `ObservableObject/@Published`.
- Приложение запускается и работает без ошибок.
