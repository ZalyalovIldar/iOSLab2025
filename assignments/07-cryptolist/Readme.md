# 🌐 CryptoList (Сетевой запрос + async/await)

Домашнее задание к лекции 7.  
Создайте приложение, которое загружает и отображает список криптовалют из открытого API.

---

## 🎯 Цели
- Научиться работать с `URLSession` и `async/await`.
- Научиться декодировать JSON через `Codable`.
- Отрабатывать состояние загрузки (loading/error/content).
- Закрепить MVVM + Observation.
- Разбивать UI на компонентные вью.

---

## 📚 Техническое задание

1. Создать модель `Crypto` (id, name, symbol, current_price, image).  
2. Реализовать сетевой сервис:
   - протокол `CryptoService`
   - реализация `RealCryptoService` с `URLSession.shared.data(from:)`
3. ViewModel:
   - загрузка данных `load() async`
   - поля: `items`, `isLoading`, `error`
4. Главный экран:
   - `.task { await vm.load() }`
   - список криптовалют: иконка, название, цена
   - обработка состояний:
     - loading
     - error + retry
     - empty
     - content
5. Минимум 2 отдельные вьюхи:
   - список  
   - строка криптовалюты  

API:  
`https://api.coingecko.com/api/v3/coins/markets?vs_currency=usd`

---

## 💻 Уровни выполнения

### Base
- Загрузка и отображение данных.
- Обработка ошибок.
- Экран разбит на отдельные вьюхи.

### Plus
- Кэширование результата (не запрашивать повторно).
- Сортировка по цене.
- Форматирование цены.

### Pro
- Использовать `AsyncImage` с placeholder.
- Добавить pull-to-refresh.
- Секция «Top Gainers».

---

## ✅ Критерии приёмки
- Данные загружаются и отображаются.
- Состояния UI работают правильно.
- Используется Observation (`@Observable`, `@Bindable`).
- Приложение не падает и выглядит аккуратно.
