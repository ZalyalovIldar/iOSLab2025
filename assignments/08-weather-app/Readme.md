# ☁️ Weather Cities (Несколько запросов + параллельность)

Домашнее задание к лекции 8.  
Создайте приложение, которое загружает погоду для нескольких городов параллельно.

---

## 🎯 Цели
- Освоить параллельную загрузку данных (`async let`, `TaskGroup`).
- Научиться выполнять цепочки сетевых запросов.
- Освоить retry-подходы.
- Реализовать расширенные состояния UI.
- Научиться добавлять поиск и сортировку поверх загруженных данных.

---

## 📚 Техническое задание

1. Создать модели:
   - `City` (name, latitude, longitude)
   - `WeatherResponse`
   - `CityWeather` (city + температура + ветер и т.п.)
2. Создать сервис `WeatherService`:
   - метод `fetchWeather(for:) async throws -> CityWeather`
3. ViewModel:
   - массив `cityWeather`
   - состояние: loading/error/content
   - загрузка в `.task`
4. Главное окно:
   - список городов и их температуры
   - индикатор загрузки
   - кнопка retry при ошибке
5. Минимум две отдельные вьюхи:
   - список
   - строка города

API: любое открытое апи погоды
---

## 💻 Уровни выполнения

### Base
- Загрузка данных по каждому городу.
- Отображение состояния загрузки / ошибки.
- Список городов.

### Plus
- Поиск по названию.
- Сортировка по температуре.
- Показывать цвет температуры (красный > 25°).

### Pro
- Параллельные запросы (`async let` или `TaskGroup`).
- Анимация появления.
- Добавить pull-to-refresh.

---

## ✅ Критерии приёмки
- Погода загружается корректно.
- Ошибки обрабатываются корректно.
- Приложение отзывчивое и не падает.
- Используется Observation, а не Combine.
