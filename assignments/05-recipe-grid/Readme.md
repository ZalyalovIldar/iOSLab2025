# 🧩 Recipe Grid (Adaptive Layout + Component-Based UI)

Домашнее задание к лекции 5.  
Создайте приложение, которое отображает сетку карточек рецептов с адаптивной версткой, возможностью добавления и удаления рецептов, а также поиском по данным. Упор — на работу с `LazyVGrid`, разбиение интерфейса на компоненты и построение простого data-driven UI.

---

## 🎯 Цели
- Освоить использование `LazyVGrid` и адаптивных колонок.
- Научиться разбивать экран на независимые компоненты.
- Закрепить работу со state (`@State`) и передачей данных вниз по дереву.
- Создать простой data-driven интерфейс без MVVM.
- Научиться добавлять данные через `.sheet`.

---

## 📚 Техническое задание

### Реализовать главный экран:
- `ScrollView`
- `LazyVGrid` с адаптивными колонками  
- Поиск по названию или категории

### Создать компонент карточки рецепта (`RecipeCardView`):
- изображение  
- название  
- категория  
- краткое описание  

### Реализовать добавление рецепта:
- через `.sheet`
- поля: `title`, `imageName`, `summary`, `category`
- обязательная валидация непустого `title`

### Реализовать удаление рецепта:
- через `contextMenu`
- или кнопкой внутри карточки

### Обработать пустое состояние:
- отдельная вьюха: `EmptyStateView`

### Минимум три отдельные вьюхи:
- `RecipeCardView`
- `AddRecipeSheet`
- `EmptyStateView`

### Модель данных:
```swift
struct Recipe: Identifiable, Hashable {
    let id: UUID
    let title: String
    let imageName: String
    let summary: String
    let category: String
}

## 💻 Уровни выполнения
### Base
- Отображение сетки рецептов.
- Поиск по названию и категории.
- Добавление новых рецептов.
- Удаление рецептов.
- Экран разделён на отдельные компоненты

### Plus
- Сортировка рецептов (по категории или алфавиту).
- Placeholder для отсутствующей картинки
- Анимация появления карточек.
- Поддержка горизонтальной ориентации (adaptive layout).

### Pro
- Фильтрация по категориям (Picker / SegmentedControl).
- Сохранение рецептов в UserDefaults.
- Редактирование существующего рецепта через sheet.
- Детальный экран с большим изображением и полным описанием.

---

## ✅ Критерии приёмки
- Рецепты можно добавлять и удалять.
- Поиск корректно фильтрует список.
- Используются современные SwiftUI-компоненты (LazyVGrid, .sheet, .contextMenu).
- Приложение работает стабильно и без ошибок.
